{% block body %}

    <h2>Modifier une formation</h2>

    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.repository) }}
    <input type="submit" value="Valider">
    {{ form_end(form) }}

{% endblock %}